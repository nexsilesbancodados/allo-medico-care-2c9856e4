import { Navigate } from "react-router-dom";
/** Guest consultation removed — login required. */
const GuestConsultation = () => <Navigate to="/paciente" replace />;
export default GuestConsultation;
